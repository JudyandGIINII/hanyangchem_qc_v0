# KANBAN — 한양화학 v0

> 정본 실행 보드는 Hermes Kanban `hanyang-chemical-v0`이다. 이 문서는 프로젝트 내 사람이 읽을 수 있는 mirror다.

## 현재 작업

|Card|상태|내용|완료 조건|
|---|---|---|---|
|`t_7d493a1e`|Completed|PRD 기반 독립 계획 2개 비교·통합·문서화|Opus 5 증빙 확인, 비교표/통합 계획/전수 추적, Alfred R1 formal+substantive PASS, DEVLOG/링크 검증 완료|
|`t_715483aa`|Completed|P0B correction candidate final independent review/acceptance|P0A/P0B complete·accepted, final independent `APPROVE` 확인|
|P1-local-candidate|Completed / accepted|P1 repository/contract foundation|User-authorized Hermes direct final QA passed the P1 contract gate and enabled the subsequently accepted P2 source increment|
|FE8-frontend-fixture|Completed / independently verified / delivered to `origin/main`|Deterministic synthetic eight-flow frontend workflow|Source/UI approvals, post-doc and fresh integration full gates, standalone/desktop/390px QA, active probe, remote-race, push and post-push verification passed; not P2/P3 backend/domain completion|
|P2-domain-db-candidate|Completed / accepted / committed / fresh-main integrated / delivered to `origin/main`|P2.1–P2.8 pure domain and DB invariants|Under separate explicit user authorization, source commit `996056b` and first integration-documentation commit `58e963c` were delivered from fresh `origin/main` baseline `1e96836`; both remain durable historical ancestors. N-M3 remains an accepted follow-up before production DB-role activation.|
|P3-vertical-slice-candidate|Completed / accepted / committed / fresh-main fast-forward integrated / delivered to `origin/main`|Synthetic calcium-chloride-bead PostgreSQL/FastAPI/Next.js vertical slice|Source commit `91465f0413d0c0ca2633577078ec1300a6096442` (52 files, +8911/−119) was fast-forwarded without merge commit/rebase from clean baseline `b7bc4a8ca258d1d44d240f8884a4b4ec8cbb6abf`. Fresh integrated bootstrap/check, Ruff, mypy 39, backend 346/77, frontend 32/build, migration 4, scans/Compose, P2/P3 PostgreSQL 10/67, and Playwright 3/3 passed. Push and post-fetch equality/ancestry checks passed; both worktrees were clean before this docs-only reconciliation, and cleanup is 0/0/0 with n8n untouched. N-1/N-2/N-5 and P2 N-M3 remain accepted non-production debt; no deployment/release/production-readiness claim.|
|P4-kickoff-preparation|Completed / historical|Documentation-only lane split, metric contract, approval packets, ownership/path plan, verification and stop/rollback plan|Historical preparation contract was synchronized and then executed for P4-A only; its old “unstarted” status is superseded by the remediation evidence below|
|P4-A-offline-synthetic|`COMPLETE_ACCEPTED_COMMITTED_FRESH_MAIN_FF_INTEGRATED_DELIVERED_TO_ORIGIN_MAIN`|Generated non-sensitive synthetic evaluator plus pre-P4-B geometry-validation hygiene|Original baseline `aeedceb…`; docs ancestors `1d98e4c…`/`afe58a0…`; four-path maintenance `cad1ab4…` accepted `ACCEPT` 0/0/0 and delivered by fresh-main non-force fast-forward. Former schema Decimal-context/dead-counter notes are closed; strict required geometry/fail-closed behavior remains. No P4-B/P4-C/full-P4 claim.|
|P4-preflight-contracts|`COMPLETE_ACCEPTED_COMMITTED_FRESH_MAIN_FF_INTEGRATED_DELIVERED_TO_ORIGIN_MAIN`|Offline/local fail-closed P4 preflight and local-pilot eligibility contracts|Source/delivery `fce19681…`; Agy/Gemini independent `ACCEPT` 0/0/0; preflight 97, golden 192, backend 607/77, mypy 52/0, frontend 32/build, migration 4, scans/Compose; fresh ff-only/non-force remote equality. No P4-B/P4-C approval or external call.|
|P4-local-only-ocr|`COMPLETE_ACCEPTED_COMMITTED_FRESH_MAIN_FF_INTEGRATED_DELIVERED_TO_ORIGIN_MAIN`|Native-text-first, local PaddleOCR low-quality PDF engineering|Source/integration baseline `91fd4a8…`; final independent `ACCEPT_WITH_MINOR` 0/0/4 with 8 notes; B1/M1-M9 and MA-1 closed. Final controller evidence: backend 641/92, mypy 67, frontend 32/build, golden 198, preflight 97, local 43, migration 4, scans/Compose, and post-remediation smoke `581ed7…37c1` / `654511…6a9c`, 1.0000/1.0000/1.0000, network 0/0. Four bounded minors remain disclosed: broad native table signal, fake-backend-only native low-confidence wiring, independent-review-time smoke non-rerun before the later controller run, duplicate native word extraction.|
|P4-public-synthetic-demo|`PRODUCTION_FRONTEND_BOUNDARY_VERIFIED_EXACT_DEPLOYMENT`|Vercel synthetic frontend-only boundary|Verified 2026-08-03 KST: `hanyangchem_qc` production `dpl_2AJpKy3L7ZLiBgEx3LRqXnxDBb7Y` is `READY` at `https://hanyangchem-739r15g9t-judy-ng-ii-nii-s-projects.vercel.app`; alias `https://hanyangchemqc.vercel.app`; meta `sourceCommit=cf6d6327172fb09da0fe0e3b12159f6596553c41`. Next.js 15.5.22 compile/lint/type, five static routes and `/api/health` completed in 57s; root 200/health ready. Alias browser QA: public boundary/title, no failed fetch or localhost resources, alias-only resources, console 0, and local-only team-lead approval. Production/Preview flags saved. `96413d2…` review remains `ACCEPT_WITH_MINOR` 0/0/6 with all six residuals unfixed. Future missing/incorrect config can fall back to localhost and must repeat deployment-API/browser proof. Backend/DB/worker/OCR/models/original documents remain local/intranet-only; later docs-only descendant has `frontend/` unchanged from `cf6d632…`, so no rebuild solely for unchanged bytes.|
|P4-local-ocr-dashboard|`DELIVERED_TO_ORIGIN_MAIN_PREVIOUSLY_UNDOCUMENTED`|Human-reviewed local OCR dashboard flow plus CI OCR bootstrap|`faee4d9` (31 files, +4107/−221) adds `DigestOwnershipGuard` digest-scoped ownership serialization (256 in-process stripes plus a PostgreSQL session-level advisory lock on a dedicated connection), the `POST/GET/PUT /api/v1/documents/{document_id}/extractions[/{run_id}]` surface, and mandatory human review where every field needs a reason and final string and `local-paddleocr` runs need an explicit `MAP`/`UNMAPPED` disposition. `1a25665`/`42c6123` add `--extra local-ocr` and `fonts-noto-cjk` to CI only. Verified this increment: `test_runtime_components.py` uses only `RecordingOcrEngine`, never `PaddleOcrEngine`, so CI still needs no model binaries and makes no network call. Recorded as delivered history; no new approval claimed.|
|P4-residual-minor-followup|`COMPLETE_ACCEPTED_COMMITTED_FRESH_MAIN_FF_INTEGRATED_DELIVERED_TO_ORIGIN_MAIN_AND_REDEPLOYED` / minors only|Closure of disclosed local-OCR and public-demo minors|Local OCR: duplicate native word extraction and over-inclusive native table detection closed (12.0pt gutter cell collapsing, pinned by a non-vacuous both-directions regression); the native low-confidence path is substantiated as unreachable via real native text, so the fake backend remains the sole way to exercise it. Public demo, all six residuals addressed: vacuous approval assertion, absent runtime fetch-spy coverage, and formatting-brittle source slicing closed via `happy-dom` mount tests with a positive `publicDemo=false` control; server-oriented public status copy closed (`합성 로컬 상태 · …`, local-API branch unchanged); `.env.example`→Compose closed via `web.build.args` plus Dockerfile `ARG`/`ENV` before `pnpm build`, default `0` so the local stack keeps its backend, pinned by `backend/tests/contract/test_public_demo_build_contract.py`; `vercel.json`/`.vercelignore` partially close config pinning since Root Directory and Framework Preset stay dashboard-only and are documented instead. Controller-re-run evidence: backend 688 passed/125 deselected, Ruff clean, strict mypy 53/0, frontend Vitest 43/5 files, lint/typecheck/build, frozen lockfile, `docker compose config`, scans. Fast-forwarded onto fresh baseline `42c6123…` with no merge commit and pushed non-force; at capture time local `main`, `origin/main`, and `ls-remote` all equaled `290e7b5…`. Full gates on the delivered tree: `make check` exit 0 (mypy 68, backend 671/142, frontend 43/5 plus build, migration 4, scans, compose), P2 PostgreSQL 10, P3 PostgreSQL 115, P3 Playwright 3/3, P4 golden 199, P4 preflight 97, Docker residue 0/0/0. `p3-e2e` needs `COMPOSE_BAKE=false` because the non-ASCII repo path breaks Docker bake (environment limit, not a defect). `p4-local-ocr-preflight` fails closed with `LOCAL_OCR_MODEL_MISSING`/`BLOCKED` as designed since no models are bootstrapped. Redeployed to production as `dpl_ER31Q8WjXup6Khuewsj5AmJ7hacr` (`READY`), alias `hanyangchemqc.vercel.app`, CLI-deployed from `frontend/`; live root 200, health ready, `합성 로컬 상태` present proving the build-flag pin and copy fix shipped, zero non-static network requests in a real browser, and the only console error is a pre-existing `favicon.ico` 404.|
|P5-scope-plan|`SCOPE_PLAN_ONLY`|P5 Core MVP 범위 분류|P5는 계획서가 없었고 traceability matrix의 21개 `P5` 행으로만 존재했다. [`plans/2026-08-10-p5-core-mvp-scope-plan.md`](plans/2026-08-10-p5-core-mvp-scope-plan.md)가 21개 행을 구조적/순차적/승인대기로 분류한다. 정책을 담은 4개 행(FR-SPEC-003, FR-SPEC-007, FR-MAP-003, FR-OCR-001)은 QUALITY/AP-02 승인 부재로 닫힌 상태이며, 샘플·판정 정책을 임의로 만들지 않았다. 이 문서는 P5 승인을 부여하지 않는다.|
|P5-master-data-slice|`IMPLEMENTED_FIRST_STRUCTURAL_SLICE`|FR-MST-001/002/003/005 API 표면|P2가 이미 `suppliers`/`materials`/`material_models` 테이블을 제공하므로 신규 테이블·마이그레이션 없이 `backend/src/hyc_api/routes/masters.py`만 추가했다. 갱신은 `with_for_update()` 행 잠금과 `If-Match` 기반 `lock_version` 낙관적 잠금을 쓰고 stale 버전과 코드 중복 모두 409로 매핑한다. soft delete 유지, hard delete 없음. nullable 코드 의미(다중 NULL 허용, 중복 non-null insert/update 거부)를 3개 엔티티 파라미터화 회귀로 고정했다. `run_p2_postgres_tests.sh`가 단일 파일만 선택해 새 db 테스트가 어떤 게이트에서도 실행되지 않던 문제를 디렉터리 선택으로 수정했다. 증거: `make check` exit 0(mypy 69/0, backend 671/148, frontend 43/5, migration 4, scans, compose), P2 PostgreSQL 13(이전 10), P3 PostgreSQL 118(이전 115), P4 golden 199, P4 preflight 97, Docker 잔여 0/0/0. P5 완료 아님.|
|P5-spec-lifecycle-slice|`IMPLEMENTED_SECOND_STRUCTURAL_SLICE`|FR-SPEC-002 / FR-MST-004 + 마스터 데이터 읽기 UI|P2가 이미 `spec_versions`의 status/effective 제약과 `spec_profiles`의 material·supplier·model scope를 제공하므로 신규 테이블·마이그레이션 없이 `routes/specs.py`만 추가했다. activate는 DRAFT만, retire는 ACTIVE만 허용해 불법 전이를 409로 거부하고, profile 행 잠금 + 경쟁 ACTIVE 행 `with_for_update()` 조회로 profile당 ACTIVE 1건 불변식을 동시성 하에서 보장한다(패자 409). 정책은 만들지 않았고 FR-SPEC-003/007은 건드리지 않았다. 프론트는 마스터 데이터 읽기 뷰를 추가하고 null 코드 placeholder를 명시 렌더하며, `canUseBackend` 가드로 publicDemo=true에서 fetch 0회를 회귀로 고정했다(publicDemo=false 양성 대조군 포함). 이전 증분에서 `run_p2_postgres_tests.sh` 수정 후 `make check` 재실행을 누락해 승인 fixture 다이제스트 검증이 깨진 채 푸시됐던 문제를 다이제스트 갱신으로 복구했다. 증거: `make check` exit 0(mypy 70/0, backend 671/151, frontend 47/6, migration 4, scans, compose), P2 PostgreSQL 16(이전 13), P3 118, P4 golden 199, preflight 97, Docker 0/0/0. P5 21행 중 6행 완료, 완료 아님.|
|P5-module-feature-flags|`IMPLEMENTED_THIRD_STRUCTURAL_SLICE`|FR-NCR-004 모듈 feature flag + 상속 4행 재확인|`config.py`의 `ncr_feature_enabled`는 어디에서도 참조되지 않고 `.env.example`에도 없는 dead flag였다. PRD 요구(부적합 보고서·승인자·재검사·첨부·완료일을 모듈별 활성/비활성)에 맞춰 5개 플래그로 세분화하고 전부 기본 false로 두었으며 `.env.example`에 추가했다. 게이트인 "invariant guard 비활성 불가"는 두 가지 구조로 강제한다: `module_exposure.py`의 frozen `ModuleExposureFlags`가 UI 노출 전용으로 격리되고 `GET /api/v1/feature-flags`로만 노출되며 도메인·DB 가드로 전달되지 않는다. `test_feature_flags.py`는 `itertools.product`로 **32개 조합 전수**를 돌며 실제 도메인 엔진에서 `review_required`, `HUMAN_REVIEW_REQUIRED`/`LOW_CONFIDENCE`/`MISSING_REQUIRED`, 미매핑·저신뢰 `ON_HOLD`가 유지됨을 확인하고, 플래그 식별자가 도메인·승인·증거 소스에 등장하지 않음을 구조적으로 단언한다. 잔여 약점: 같은 테스트의 일부 소스 문자열 단언(`session.add_all` 등)은 이전에 닫은 formatting-brittle 유형이라 해당 영역 수정 시 교체 필요. 재확인 결과 매트릭스 인용 오류 2건 발견: FR-INT-002가 인용한 `test_internal_results_api.py`는 미존재이며, 워커가 근거로 든 `REQUIRED_INTERNAL_ITEM_CODES`는 저장소 전체에 존재하지 않는다(실제는 blocker 기반 `INTERNAL_TEST_PENDING`). `SamplePolicy`는 6종 StrEnum으로 메커니즘은 있으나 품목별 정책 배정이 FR-SPEC-007 QUALITY 게이트라 FR-INT-003은 계속 보류. 증거: `make check` exit 0(mypy 72/0, backend 705/151, frontend 47/6, migration 4, scans, compose), P2 16, P3 118, P4 golden 199, preflight 97, Docker 0/0/0. P5 21행 중 7행 완료, 완료 아님.|
|P5-ncr-module-schema|`IMPLEMENTED_SCHEMA_ONLY`|FR-NCR-001 / FR-NCR-002 스키마 + FR-INT-006 저장 계층|승인된 설계는 [`plans/2026-08-10-p5-ncr-module-schema-design.md`](plans/2026-08-10-p5-ncr-module-schema-design.md). 사전 조사로 정정: FR-NCR-003 재검사 연결은 `inspection_cases`의 `retest_of_case_id`/`lineage_root_id`/`round_no`/`revision_no`와 관련 CHECK·UNIQUE로 **이미 구현돼 있어 신규 테이블 불필요**하며 FK 참조만 한다. `documents`를 첨부에 재사용해 SHA-256 중복 제거·불변성을 승계하므로 신규 파일 저장소를 만들지 않았고 FR-INT-006이 함께 충족된다. 기존 `approvals`는 `LEAD`/`APPROVE` CHECK 고정이라 재사용 불가해 별도 승인 테이블을 두되 `actor_role='LEAD'`를 DB CHECK로 강제(AP-04 일관). 단일 리비전 `20260810_0005`(부모 `20260801_0004`)는 마이그레이션 metadata 독립성 계약 때문에 순수 DDL이다. 트리거 2종으로 CHECK가 표현 못하는 요구를 DB에서 보장: disposition DELETE 거부(과거 기록 유지, 비활성화만 허용)와 APPROVED 부적합 불변성. `check_migrations.py`가 두 트리거 존재를 계약으로 검증한다. `disposition_snapshot`은 마스터 비활성화·개명 시 과거 기록 의미 보존용(P3 `spec_snapshot` 패턴). severity 기준·목표 완료일 산정·처리방안 선택 규칙은 일절 만들지 않았다. FR-MAP-001은 롤백 범위 축소를 위해 제외. API/UI 미포함. 증거: `make check` exit 0(mypy 73/0, backend 705/153, frontend 47/6, migration 4, scans, compose), P2 PostgreSQL 18(이전 16, 왕복·drift 포함), P3 118, P4 golden 199, preflight 97, Docker 0/0/0.|
|P5-ncr-api-ui|`IMPLEMENTED_API_AND_UI`|FR-NCR-001 / FR-NCR-002 API + UI|`routes/nonconformances.py`가 disposition 목록(`include_inactive` 플래그로 비활성 값도 조회 가능, 과거 기록 가시성 유지), NCR CRUD, approve/reject, 첨부 링크 관리를 제공한다. approve/reject는 `require_role(principal,"LEAD")`로 403을 반환해 AP-04 원칙이 DB CHECK에만 의존하지 않게 했다. 생성 시 `disposition_snapshot`에 code/name을 고정해 마스터 개명·비활성화가 과거 기록 의미를 바꾸지 못하게 한다. UI는 스냅샷 기준으로 처리방안을 표시하고, severity 부재 시 placeholder를 렌더하며, APPROVED면 편집을 비활성화하고, 403·409를 읽을 수 있는 메시지로 노출한다. `canUseBackend` 가드로 publicDemo=true에서 fetch 0회를 회귀로 고정(양성 대조군 포함). **검토 중 결함 발견·수정**: `_commit`이 광범위한 `DBAPIError`를 전부 409로 매핑해 연결 끊김·타임아웃 같은 인프라 장애가 업무 충돌로 위장되고 무의미한 재시도를 유발할 수 있었다. 이 저장소는 트리거 불변식을 23곳에서 `RAISE EXCEPTION`(SQLSTATE `P0001`)으로 강제하므로 `P0001`만 409로 좁히고 나머지는 그대로 전파하도록 고쳤다(기존 관례가 없어 이 파일이 첫 사례). 증거: `make check` exit 0(mypy 74/0, backend 705/157, frontend 54/7, migration 4, scans, compose), P2 18, P3 **122**(이전 118), P4 golden 199, preflight 97, Docker 0/0/0.|
|P5-alias-and-return|`IMPLEMENTED_LAST_NON_GATED_ROWS`|FR-MAP-001 별칭 / FR-APR-003 반려|리비전을 `20260810_0006`(별칭)과 `20260810_0007`(반려 이력) 둘로 분리해 각각 독립 롤백 가능하게 했고 둘 다 순수 DDL이다. 별칭은 `standard_test_item_aliases`에 품목·공급사·모델 optional scope(기존 `spec_profiles`와 동일 패턴), priority, active를 두고 동일 scope 내 중복 alias_text를 거부하며 priority→text 결정적 정렬을 보장한다. **전역 승격·자동승인·학습·신뢰도 가중 컬럼은 의도적으로 만들지 않았다** — FR-MAP-003이 QUALITY 게이트이므로 스키마에 여지를 두지 않는다. 별칭은 후보 추천 전용이며 매칭을 자동 확정하지 않고, UI가 "후보 추천 전용 · 수동 확정 필요"를 명시하며 자동 확정으로 표기하지 않음을 테스트로 고정했다. 반려는 재제출→재반려가 반복되므로 컬럼이 아니라 append-only 이력 테이블 `inspection_return_reasons`로 모델링했고 `reason TEXT NOT NULL`로 필수 사유를 DB가 강제한다. `POST /inspections/{id}/return`은 LEAD 전용(403), 사유 누락 시 422, 상태를 RETURNED로 전이한다. 신규 라우트는 이전 증분의 `P0001` 한정 `DBAPIError` 매핑을 재사용한다. 프론트는 scope 부재 시 전체 범위 placeholder, 사유 비었을 때 제출 비활성화, 403·422 메시지 노출, `canUseBackend` 가드로 publicDemo=true fetch 0회(양성 대조군 포함). 증거: `make check` exit 0(mypy 77/0, backend 705/160, frontend 61/8, migration 4, scans, compose), P2 18, P3 **125**(이전 122), P4 golden 199, preflight 97, Docker 0/0/0. **비게이트 P5 작업은 이로써 완료이며, 남은 전 항목은 QUALITY/AP-02 승인 대기로 엔지니어링 이슈가 아니다.**|
|P6-scope-plan|`APPROVED_SCOPE_PLAN_ONLY`|P6 Operations 범위·설계 확정|2026-08-11 사용자 결정으로 **P6 전체 승인**. 범위·설계는 [`plans/2026-08-11-p6-operations-scope-plan.md`](plans/2026-08-11-p6-operations-scope-plan.md)가 정본이다. 7조각으로 분할: P6-1 보고서 공통 틀+통합검사보고서, P6-2 LOT추적·통계·통계화면·Raw Data 엑셀, P6-3 부적합 후속조치, P6-4 NAS/Drive 수집 준비, P6-5 마스터 Import 준비, P6-6 OCR 운영 모니터링, P6-7 백업/복구. 앞 3조각은 완성품, 뒤 4조각은 **seam 준비**다. 승인의 성격은 "만들어라"이지 "없는 실물을 만들어내라"가 아니다 — 사용자가 COA 샘플·NAS 접속·Import용 실 엑셀 부재를 명시하고 "준비만 해"를 지시했다. 준비의 정의는 4조건으로 고정: 포트/계약 확정, 합성 구현체로 CI 통과, 어댑터 1개로 국소화, 플래그 off 기본에 부수효과 0. **보존 정책(RET-001)은 사용자 결정으로 제외**하고 백업/복구(BACKUP-001)만 포함한다. Feature Flag는 FR-NCR-004로 이미 완료라 재작업 없음. 이 승인은 실제 NAS/Drive/ERP 호출, 실 PDF/XLSX 커밋(AP-05), 외부 Provider 호출(AP-02), production migration/DB-role, 보존기간·삭제 규칙, release 선언을 부여하지 않는다. 승인으로 풀리지 않는 사실상의 제약도 기록: OCR KPI 임계값은 PRD §3.3이 "초기 운영 데이터 확보 후 기준선 측정"을 요구하므로 COA 샘플 없이 만들지 않으며, P5 잔여 5행(FR-SPEC-003/007, FR-MAP-003, FR-OCR-001, FR-INT-003)의 QUALITY/AP-02 게이트도 해제되지 않는다. 확정 설계 결정 4건: 첫 조각은 보고서·통계 라인, 실행은 Job 리소스+in-process `ReportRunner` 포트(PRD `report async`/`Job ID` 충족, Redis 큐·DLQ는 범위 밖), 산출물은 별도 `report_artifacts`+StoragePort 재사용(ARCH-003 원본/파생 구분, 삭제 경로 미생성), 보고서 출처는 스냅샷/조회시점 **이원화 후 문서에 명시 렌더**. 이 문서는 commit/push/deploy 권한을 부여하지 않는다.|
|P4-B-approved-corpus|`BLOCKED_QUALITY_CORPUS_APPROVAL` / future validation gate|Representative real-corpus benchmark, only if desired|Inventory 4 candidate documents/0 eligible; packet remains `PENDING / NOT APPROVED`. Obtain human-label and independent-review evidence before any run. This is not unfinished code debt.|
|P4-C-external-provider|`BLOCKED_AP02_PROVIDER_OPT_IN` / deferred, not required for local-only|Future external Provider benchmark and selection|Packet/research retained; no Provider selected/approved and no account/credential request is active. No external call.|

## 검증 결과

- Claude Code canonical model: `claude-opus-5`, `is_error=false`
- Alfred 1차 findings: HIGH 3건·MEDIUM 2건, 총 5건을 보정했고 R1에서 모두 `RESOLVED`
- Alfred R1: 신규 HIGH/MEDIUM blocker 0, `substantive_plan_assessment: PASS`
- P0B second remediation: independent re-review의 HIGH 1건·MEDIUM 3건(OOXML/OPC semantic validation, every-`.rels` validation, all-member CRC/decompression, source-digest provenance binding)을 synthetic regression coverage로 보정했으며 재검토/controller acceptance 대기
- P0B third focused remediation: MEDIUM Content-Type-driven OPC member validation 및 complete Relationship element semantics, LOW root-level relationship-source compatibility를 synthetic-only regression으로 보정했다. `pytest -q`는 2026-07-31에 `94 passed in 4.41s`; final QA/controller acceptance 대기 상태는 변함없다.
- P0B fourth focused remediation: MEDIUM full RFC 3986 Relationship-Type lexical validation 및 canonical OPC member/Override/relationship-Target lexical validation, LOW attribute-free `Relationships` root hardening을 synthetic-only regression으로 보정했다. 서로 다른 typed relationship ID의 동일 target은 OPC 호환성을 위해 허용하며 duplicate ID만 거부한다. `pytest -q`는 2026-07-31에 `127 passed in 5.90s`였다.
- P0B final independent review: 67 in-memory probes, HIGH 0, MEDIUM 0, `APPROVE`. generic scheme-specific URI semantics의 LOW lexical-contract note는 소비 relationship role이 exact allowlist를 사용하므로 defense-in-depth로 수용됐다.
- Controller evidence: `127 passed`; approved real QM301 dry-run 38 templates/119 rows, discrepancy 0, DB write/apply 0; source hash/size/mtime unchanged; tracked sensitive documents 0.
- P1 final Hermes direct QA (explicitly authorized by the user in place of unavailable Claude reapproval): root `pytest.ini` is canonical and `--collect-only -vv` confirmed repository rootdir/configfile; direct targeted test file 12 passed; full suite 172 passed with one non-blocking upstream Starlette/httpx deprecation warning. Final `make check` exited 0 for contract/client drift, Ruff, strict mypy (15 files), pytest, compileall, frontend lint/typegen/typecheck/Vitest (1)/build, migration, secret/sensitive scans, and Compose config. Frontend Corepack pnpm runs from frontend cwd with pinned pnpm `10.13.1`; typecheck is `tsc --noEmit --incremental false`, no `tsconfig.tsbuildinfo` remains, frozen install and all gates passed, and the lockfile is unchanged. Final exact-candidate Compose controller process `proc_7e03db110d2f` exited 0 with all five services healthy, expected API/web probes, PostgreSQL migration roundtrip, and cleanup passing.
- FE8 frontend fixture closure (pre-docs): eight deterministic synthetic flows are implemented and independently approved on source (`APPROVE`, BLOCKER/MAJOR/MINOR 0) and UI (`APPROVE`, HIGH/MEDIUM/LOW 0). The source-only 10-file manifest was `5b7f222fa5bba991499c3be4e8b49231fba59bb66b1b50dcc1e43ed29ddb6335`, with unchanged review status digest `754834d90ae0c30075a0e611383abc9f99e6c157d335c7e6ddcd6ff8cb569692`; it is not docs-inclusive final evidence. Local Decimal-string/BigInt, explicit confirmation, fail-closed/reducer-only frozen snapshot, and submission/approval locks were verified. Roles are simulation only; no backend mutation, persistence, real auth, OCR/AI, ERP, apply/import, or deployment occurred. Controller `make bootstrap && make check` exited 0 (backend pytest 172; frontend Vitest 3 files/32 tests; stated static/build/scans), and the 390×844 viewport had no page-level overflow.
- P2 final minor hardening source: N-M1 adds immutable Alembic head `20260801_0003`, requiring every new inspection case to be unfinalized, while frozen `20260731_0002` retains SHA-256 `546acd12aff2778c9ee6b6a11f8d24f87417dc8a792945f468971011a43c6f82`. N-M2 keys document transitions by current+target+role with duplicate-key detection; the supplier-only/model-only equal-rank ambiguity has a dedicated fail-closed unit regression. `make check` passed with backend 346 passed/10 PostgreSQL deselected, strict mypy 29 files, migration contract 4, and FE8 frontend 32; the disposable PostgreSQL runner separately passed 10 tests plus migration/autogenerate roundtrip and verified cleanup. The earlier 344/9/28 and PostgreSQL 9 measurements remain historical and are superseded by this current evidence.
- P2 final acceptance: Hermes independently reproduced `make check` exit 0, backend `346 passed, 10 PostgreSQL deselected`, strict mypy 29 files, migration contract 4, FE8 frontend 32 plus lint/typecheck/build, scans/Compose, PostgreSQL 10 plus upgrade→downgrade→upgrade and empty drift, and cleanup 0/0/0. Final Claude report `/tmp/hyc-p2-absolute-final-claude-review.md` returned `PASS` with BLOCKER 0, MAJOR 0, MINOR 1 and closed B1–B5, M1–M9, m1–m7, H1, N1–N3, N-M1, and N-M2.
- P2 post-integration closure: P2 is complete, accepted, committed, fresh-main integrated, and delivered to `origin/main` under separate explicit user authorization. Source commit `996056b` and first integration-documentation commit `58e963c` were delivered from fresh baseline `1e96836`; `58e963c` is verified first-push/integration evidence and a durable ancestor. Fresh P2 integration `make bootstrap` and `make check` exited 0 with backend 346 passed/10 PostgreSQL deselected, strict mypy 29, migration 4, FE8 frontend 32 plus lint/typecheck/build, scans/Compose; disposable PostgreSQL 10 passed and cleanup was verified.
- Accepted follow-up N-M3 is not fixed: a DB-direct app-role writer with broad direct `inspection_cases` plus evidence-table privileges can insert an unfinalized case already at `LEAD_REVIEW` and finalize it with complete valid evidence, bypassing intermediate status history. N1 decision integrity, mandatory evidence, and finalized-row immutability still hold. Review this defense-in-depth gap before any production DB-role activation.
- P3 second-remediation builder verification: repeated confirmation with the same or another allocation is 409 and cannot rewrite fields or add a second confirmed link; PostgreSQL independently rejects a second confirmed link. Internal-results PUT performs full replace/clear, stale versions conflict, and clear restores the fail-closed hold/submit block. GET issues no mutation statement, and P3 storage cleanup removes its exact trap-owned tree. `make check` passed backend 346/23 deselected, strict mypy 39, frontend 32, migration 4, generated/security/Compose gates; P2 PostgreSQL passed 10, P3 PostgreSQL passed 13, and loopback Playwright passed 3/3. N-1/N-2/N-5 remain disclosed fixture-only follow-ups.
- P3 last-blocker remediation builder verification (historical checkpoint): independent QA reopened the true empty-section first-confirm race because the prior test pre-inserted the section. The replacement PostgreSQL barrier test began with zero sections and passed 15/15 races across three targeted invocations; checkpoint counts were backend 346/29 deselected, P2 PostgreSQL 10, P3 PostgreSQL 19, frontend 32, migration 4, and full Playwright 3/3. Each race proved one 200/one 409/no 500, exactly one section/link, full loser rollback, and unrelated residue 0. The then-pending Hermes gate is superseded by the final source acceptance below.
- P3 final idempotency/upload remediation builder verification (historical checkpoint): the red PostgreSQL run reproduced 18/18 missing-row first-reservation races as success+500 and both invalid uploads as 500. The exact `uq_idempotency_principal_scope_key` savepoint fix and typed upload errors made the focused suite 22/22 and full P3 suite 40/40; repeated same-payload pairs were 201+409/201+409/200+409, losers were stable 409, and replay was byte-identical. Checkpoint backend was 346/50 deselected with P2 PostgreSQL 10, frontend 32, migration 4, Playwright 3/3, cleanup 0/0/0/0. The then-pending Hermes gate is superseded below; N-1/N-2/N-5 remain fixture-only debt.
- P3 final independent source acceptance: backend review `PASS` with blocker/major/medium 0 after P3 PostgreSQL 67, mutation-first reverse-order 6 cycles, terminal-first focused 27, P2 regression 10, and substantive `make check` gates (backend 346/77 deselected, mypy 39, frontend 32, migration 4). UI/API review `PASS` after real Playwright 3/3, a separate live-stack desktop and 375×812 smoke, expected concurrent status pairs 201+409 intake, 201+409 inspection, 200+409 approval, byte-identical sequential replay, invalid-upload 422/413 with no residue, P3 API 67, unchanged repository fingerprint, and cleanup 0/0/0/0.
- Hermes controller acceptance evidence: `make bootstrap && make check` passed Ruff, mypy 39, backend 346/77 deselected, frontend Vitest 32 and Next build, migration 4, scans and Compose; P2 PostgreSQL 10, P3 PostgreSQL 67, real Playwright 3, and `test_db_serialization.py` 27 tests across three fresh cycles (81 total) passed. Candidate hash remained the frozen pre-doc source value, Docker HYC containers/networks/volumes were 0/0/0, and only user-owned n8n remained running and untouched.
- P3 pre-doc-final freeze: base HEAD `b7bc4a8ca258d1d44d240f8884a4b4ec8cbb6abf`, 50 changed/untracked files, source hash `51f3bbb1d23970484813e893e51fd781f89fb781d02fac2db5cb475b00cac7f2`. This is not claimed as the post-documentation hash.
- P3 post-integration closure: source commit `91465f0413d0c0ca2633577078ec1300a6096442` (`feat: complete P3 vertical slice`) contains exactly 52 files, 8911 insertions, and 119 deletions. Clean local `main`/`origin/main` baseline `b7bc4a8ca258d1d44d240f8884a4b4ec8cbb6abf` was fast-forwarded with `git merge --ff-only 91465f0...`, with no merge commit or rebase. Fresh integrated gates all passed: bootstrap/check, Ruff, mypy 39, backend 346/77, frontend 32/build, migration 4, scans/Compose, P2 PostgreSQL 10, P3 PostgreSQL 67, and Playwright 3/3. HYC cleanup was 0/0/0 and n8n was untouched. Push `b7bc4a8..91465f0 main -> main` succeeded; post-fetch local main, `origin/main`, and remote main all equal the full source commit, both base/source are ancestors of `origin/main`, and main/candidate worktrees were clean before this docs-only reconciliation.
- P4-A original final evidence (historical): exact focused selector 15/47; golden 183; Ruff; strict mypy 49/0; backend 501/77 plus compileall; full check exit 0; P2/P3 PostgreSQL 10/67; cleanup 0/0/0. Original 25-path review was `ACCEPT_WITH_MINOR` 0/0/3 with unchanged source-only/full digests `0d5c259f…db632` / `ba938518…18f23`.
- P4-A maintenance closure: exact four-path freeze digest `7ed91a67…a85ce` was unchanged before/after independent review; final `VERDICT: ACCEPT`, blocker/major/minor 0. Polygon validation uses precision 28 / `ROUND_HALF_EVEN`; public missing/invalid polygon counts remain Literal-zero compatible while dead internal counters are removed and strict required geometry/fail-closed behavior remains. Controller focused 9; golden 192; Ruff; strict mypy 49/0; backend 510/77 plus compileall; full check exit 0 with frontend Vitest 32/build, migration 4, scans, and Compose. Benchmark repeated twice with unchanged `354d7c10…bfd23` / `7b0601d2…8933` / `05f77739…18d0` digests.
- P4-A source/Git closure: `aeedceb2c3b7008439a9c72e3984be77f6135e51` (`feat: complete P4-A offline synthetic evaluator`) directly descends from `2d5c02d…`. Fresh fetched-`origin/main` integration used `git merge --ff-only` without merge commit/rebase and a successful non-force push. At capture time integration HEAD/`origin/main`/remote main equaled `aeedceb…`; ancestry and both clean worktrees passed. The commit containing this nine-file closure is a newer descendant; Git history is authoritative for its exact SHA/remote-tip status, while `aeedceb…` remains the P4-A source/integration baseline rather than the continuing tip.
- P4-A maintenance Git closure: `1d98e4c…` and `afe58a0…` are post-integration documentation ancestors; `cad1ab48b7ab1923638fe8600f23ef640efdab73` (`fix: stabilize P4-A geometry validation`) directly descends from `afe58a0…`. Fresh `origin/main` fast-forward/non-force delivery succeeded, and capture-time local integration HEAD/`origin/main`/remote main all equaled `cad1ab4…`; ancestry and clean status passed. Git history is authoritative for the exact later docs-only tip.
- P4 preflight closure: source/delivery commit `fce19681f75cac8f95bb6cde95ad50351cf9e309` directly descends from fresh `origin/main` baseline `4866f7a992cd8e40dc95b43b1b2adaa13d989752`. Agy/Gemini independent reviews returned `ACCEPT` 0/0/0. Final preflight 97, golden 192, Ruff, strict mypy 52/0, backend 607/77, frontend 32/build, migration 4, scans, and Compose passed. Fresh ff-only integration/non-force push and capture-time integration HEAD/`origin/main`/remote equality passed.
- Local inventory remains aggregate-only: 4 candidate documents, 0 eligible due to absent human-label and independent-review evidence. It is non-representative and not a QUALITY corpus. The Azure research candidate is neither selected nor approved; no account/endpoint/credential/network call exists in this increment.
- Local-only OCR closure: source/integration baseline `91fd4a8…` is accepted/delivered and pre-closure main `96413d2…` is its descendant. The closure commit will be newer and Git history is authoritative. Independent review `ACCEPT_WITH_MINOR` 0/0/4, NOTE 8 and controller evidence above are final for the source lane; no representative-corpus or production claim follows.
- 구현 상태: `P0A_P0B_P1_P2_P3_SOURCE_COMPLETE_ACCEPTED; P3_DELIVERED_TO_ORIGIN_MAIN; P4A_OFFLINE_SYNTHETIC_COMPLETE_ACCEPTED_DELIVERED_TO_ORIGIN_MAIN; P4_LOCAL_ONLY_OCR_COMPLETE_ACCEPTED_DELIVERED_TO_ORIGIN_MAIN`

## 승인 현황

|Gate|결정|상태|
|---|---|---|
|AP-01|사내망 Docker Compose·공개 배포 없음|승인|
|AP-02|외부 OCR/AI 기본 OFF 및 향후 opt-in 절차|승인|
|AP-03|canonical LOT + inbound allocation 데이터 모델|승인|
|AP-04|Local Auth/RBAC 및 ADMIN 비승인권|승인|
|AP-05|실 PDF/XLSX Git 커밋 금지·마스킹 fixture 정책|승인|
|Implementation|P0A/P0B/P1/P2/P3 source increments|P0A/P0B/P1/P2/P3 source complete·accepted; P2 and P3 committed, fresh-main integrated, and delivered to `origin/main`. Product/operations gates remain unauthorized.|
|P4-A|Offline/synthetic foundation only|Complete/accepted/delivered at original source baseline `aeedceb…` plus accepted/delivered maintenance `cad1ab4…`; no real OCR/Provider/full-P4 claim|
|P4 local OCR|[2026-08-03 implementation note](research/2026-08-03-local-only-low-quality-pdf-ocr.md)|Complete/accepted/delivered at source baseline `91fd4a8…`; four bounded minors and eight notes retained|
|P4-B corpus|[`P4-B QUALITY packet`](approvals/P4B_QUALITY_CORPUS_DECISION_PACKET.md)|`BLOCKED_QUALITY_CORPUS_APPROVAL`; future real-corpus validation gate, not code debt|
|P4-C Provider|[`P4-C Provider-specific AP-02 packet`](approvals/P4C_PROVIDER_AP02_DECISION_PACKET.md)|`BLOCKED_AP02_PROVIDER_OPT_IN`; deferred/not required for local-only; no active request|
|P6 Operations|[P6 범위·설계 계획](plans/2026-08-11-p6-operations-scope-plan.md)|**승인** (2026-08-11 사용자 결정). 7조각 전체. 단 실물 부재 3항목(OCR 모니터링·NAS/Drive 수집·마스터 Import)은 seam 준비까지이며, 보존 정책(RET-001/AP-08)은 범위에서 제외|
|Still prohibited|실데이터 apply/import, 외부 OCR/AI/NAS/Drive/ERP **실제 호출**, 실 PDF/XLSX 커밋, 비일회성 migration, 배포/서비스 공개, 보존기간·삭제 규칙, OCR KPI 임계값|미승인|

## 실행 Backlog

1. P0A read-only evidence freeze — Completed, source immutable PASS
2. P0B evidence tooling/fixture bootstrap와 ADR — Completed/accepted
3. P1 Repository/Contract foundation — Completed/accepted through user-authorized Hermes direct QA
4. P2 Pure domain + DB invariants — Completed/accepted, committed, fresh-main integrated, and delivered to `origin/main` (`996056b`, verified first-push/integration-evidence commit `58e963c`); `58e963c` remains a durable ancestor and Git history is authoritative for the live tip; N-M3 retained as pre-production-privilege follow-up
5. P3 Fixture 기반 첫 수직 Slice — Completed/accepted, committed as `91465f0413d0c0ca2633577078ec1300a6096442`, fresh-main fast-forward integrated, and delivered to `origin/main`
6. P4 local-only OCR — Completed/accepted/committed/fresh-main fast-forward integrated/delivered at source baseline `91fd4a8…`; final independent review and controller evidence are recorded in the [2026-08-03 implementation note](research/2026-08-03-local-only-low-quality-pdf-ocr.md). If representative real-corpus validation is desired, obtain P4-B QUALITY approval/evidence; otherwise hand off to the next separately approved phase. P4-C remains deferred.
7. P5 Core MVP — 21개 P5 행 중 12행 구현(FR-MST-001~005, FR-SPEC-002, FR-NCR-001/002/004, FR-MAP-001, FR-APR-003, FR-INT-006). FR-NCR-003은 기존 lineage로 충족. **비게이트 작업은 완료**이며 남은 FR-SPEC-003, FR-SPEC-007, FR-MAP-003, FR-OCR-001, FR-INT-003은 전부 QUALITY/AP-02 승인 대기로 엔지니어링 이슈가 아니다. 승인 없이는 P5 추가 진행 불가.
8. P6 Operations — **2026-08-11 사용자 결정으로 전체 승인**되어 착수 가능. 범위·설계 정본은 [`plans/2026-08-11-p6-operations-scope-plan.md`](plans/2026-08-11-p6-operations-scope-plan.md)이며 인계 배경은 `HANDOFF.md`의 2026-08-10 섹션이다. 7조각 순서: P6-1 보고서 공통 틀+통합검사보고서 → P6-2 LOT추적·통계·통계화면·Raw Data 엑셀 → P6-3 부적합 후속조치 → P6-4 NAS/Drive 수집 준비 → P6-5 마스터 Import 준비 → P6-6 OCR 운영 모니터링 → P6-7 백업/복구. 앞 3조각은 완성품이고 뒤 4조각은 seam 준비다. **이전 상태(마스터 Import·NAS/Drive 금지)는 이 승인으로 갱신됐다** — 다만 승인된 것은 코드이며 실제 접속·자격증명·실 엑셀 커밋은 계속 금지다. Feature Flag는 FR-NCR-004로 이미 완료라 재작업 없음. 보존 정책(RET-001)은 사용자 결정으로 제외. OCR KPI 임계값은 승인과 무관하게 COA 샘플 부재로 만들 수 없다(PRD §3.3). P5 미완료 5행의 QUALITY/AP-02 게이트는 계속 유효하며 P6가 이를 해제하지 않는다.

P1/P2/P3와 delivered P4-A/preflight/local-only OCR 기록은 유지된다. **현재 truthful next boundary는 P6-1 착수다** — 2026-08-11 P6 승인으로 갱신됐다. P4-B는 real-corpus validation을 원할 때만 QUALITY corpus approval과 human-label/independent-review evidence를 확보하는 별도 게이트로 남고, P4-C는 external-Provider opt-in이 별도로 요청될 때만 적용되며 현재 account request는 없다. **P5 Core MVP는 21행 중 12행 구현으로 미완료이며, 잔여 5행은 전부 QUALITY/AP-02 승인 대기다** (이전의 "P5 unstarted" 서술은 2026-08-10 P5 증분으로 대체됐다). Fixture-only N-1/N-2/N-5와 P2 N-M3는 accepted debt로 유지한다. 실데이터 apply/import, 외부 OCR/AI/NAS/Drive/ERP **실제 호출**, 실 PDF/XLSX 커밋, 비일회성/production DB, production DB-role activation, 보존기간·삭제 규칙, OCR KPI 임계값, release/production readiness는 계속 시작하지 않는다. Public synthetic Vercel demo는 별도 frontend-only 경계이며 live identity는 deployment API/browser evidence로 확인한다.
