[pytest]
testpaths = backend/tests
pythonpath = backend/src .
markers =
    postgres: requires an explicitly supplied disposable PostgreSQL DSN
    local_ocr_runtime: requires explicitly bootstrapped local OCR packages and model artifacts
